{
  "autoload": {
    "classmap": [ // pour le routeur, c une règle pour définir que pour l'autoload, tout tt les chemins de notre projets sont accessibles
      "./"
    ]
  },
  "require": {
    "ext-pdo": "*"
  }
}

// but de l'autoload: charger des fichiers